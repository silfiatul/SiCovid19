const API_ENDPOINT = require('../../global/apiEndpoint');
const CONFIG = require('../../global/config');

const Home = {
  render() {
    return `
      <h1>Home</h1>
    `;
  },
};

module.exports = Home;
